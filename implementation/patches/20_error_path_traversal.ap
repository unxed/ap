# Summary: Tests the security check that prevents path traversal attacks.
#
# Plan:
#   1. Attempt to create a file outside the project directory using `../` in the path.
#      The patch should fail with an INVALID_FILE_PATH error.
#
version: "2.0"
changes:
  - file_path: "../path_traversal_attack.txt"
    modifications:
      - action: CREATE_FILE
        content: |
          This file should not be created.
