# Summary: Tests that the patcher fails correctly when an anchor is not found.
#
# Plan:
#   1. In `09_error_src.py`, attempt to perform a replacement using an anchor that does not exist.
#      The patch should fail with an ANCHOR_NOT_FOUND error.
#
version: "2.0"
changes:
  - file_path: "09_error_src.py"
    modifications:
      - action: REPLACE
        anchor: |
          def  my_function(arg1, arg2):
        snippet: |
          print("Hello")
        content: |
          pass
